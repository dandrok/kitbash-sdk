{
  "name": "kitbash-workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "format": "bunx @biomejs/biome format --write .",
    "lint": "bunx @biomejs/biome lint .",
    "check": "bunx @biomejs/biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4"
  },
  "workspaces": [
    "packages/*",
    "templates/*",
    "sandbox"
  ]
}
